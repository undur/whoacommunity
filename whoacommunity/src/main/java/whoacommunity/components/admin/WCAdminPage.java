package whoacommunity.components.admin;

import ng.appserver.NGContext;
import ng.appserver.templating.NGComponent;

public class WCAdminPage extends NGComponent {

	public WCAdminPage( NGContext context ) {
		super( context );
	}
}